

Chúng ta sẽ thiết kế phần service cho laputa giúp nó hoạt động đúng đắng nhất



1. Api save note thông thường sẽ có lệnh bắn event liên tục, việc bắn lên mà thiếu thứ tự hoàn thành có khả năng sẽ failed nội hàng loạt, do đó cần đám bảo api save được gọi đúng thứ tự và hoàn thành đúng theo thứ tự được gọi và gửi lên
api thì sẽ có tham số là: save by id
id
title
contentHtml


2. đối với api get, chúng ta áp dung pagination, sử dụng querytype: "inbox", "all", "started", "section"
kết hợp với page, nếu có các action như sorting thì yêu cầu phải reset và gửi lại api để đảm bảo tín đúng đắng, ví dụ như khi search by text cần phải reset lại api cho nó đúng đắng nhát


các api get list phải hoàn thành theo đúng thứ tự, ví dụ khi pageination mà trả về api số 3 xong trước và api số 2 chưa xong thì phải chờ api số 2 trả về để append data vào để cho đúng thứ tự, chúng ta vẫn gọi song song để api nào chạy xong nhanh thì có thể hoàn thành và không cần latency quá lâu



question:
3. khó nhất đó làm thế nào để chúng ta có thể thực hiện hành động save trực tiếp và điều đó refect lên trên các component đang listen, bắt buộc phải sử dụng ngmodel sao? còn giải pháp nào khác không?

