

chúng ta sẽ làm api

1. tạo note controller và implement các api
2. lưu ý với api get, sử dụng model
    - chúng ta sẽ sử dụng stragy query, bằng query type chúng ta sẽ lựa chọn thể hiện tương ứng, hiện tại sẽ là các thể hiện inbox, all, category, default (null pattern)
    - với từng thể hiện nó sẽ có bộ tham số riêng, do đó khi implement cho từng thể hiện chúng ta sẽ biết được cần sài những tham số nào cho thể hiện đó
3. chúng ta sẽ lấy các note thông qua obsidian, do đó lớp repository sẽ là obsidianRepository implement cho việc lấy note
