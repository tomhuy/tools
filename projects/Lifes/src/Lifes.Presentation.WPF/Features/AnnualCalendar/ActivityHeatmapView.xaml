<UserControl x:Class="Lifes.Presentation.WPF.Features.AnnualCalendar.ActivityHeatmapView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             Background="White"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1400">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />

        <Style x:Key="HamburgerButtonStyle" TargetType="ToggleButton">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="Border" CornerRadius="6" Background="Transparent" Padding="{TemplateBinding Padding}">
                            <Path x:Name="Icon" Data="M3,6H21V8H3V6M3,11H21V13H3V11M3,16H21V18H3V16Z" 
                                  Fill="#64748B" Stretch="Uniform" Width="20" Height="20"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#F1F5F9"/>
                                <Setter TargetName="Icon" Property="Fill" Value="#1E293B"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#F1F5F9"/>
                                <Setter TargetName="Icon" Property="Fill" Value="#3B82F6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ToolMenuButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="#64748B"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border" Background="{TemplateBinding Background}" CornerRadius="4">
                            <ContentPresenter x:Name="Content" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#F8FAFC"/>
                                <Setter Property="Foreground" Value="#1E293B"/>
                            </Trigger>
                            <DataTrigger Binding="{Binding IsActive}" Value="True">
                                <Setter Property="Foreground" Value="#3B82F6"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                                <Setter TargetName="Border" Property="Background" Value="#EFF6FF"/>
                            </DataTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Mock Grid Style -->
        <Style x:Key="GridBorderStyle" TargetType="Border">
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="BorderThickness" Value="0"/> <!-- Removed borders between days -->
            <Setter Property="Background" Value="White"/>
            <Setter Property="Width" Value="34"/>
            <Setter Property="Height" Value="34"/>
        </Style>
        
        <Style x:Key="HeaderBorderStyle" TargetType="Border">
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="BorderThickness" Value="0"/> <!-- Removed borders between days -->
            <Setter Property="Background" Value="#F9FAFB"/>
            <Setter Property="Width" Value="34"/>
            <Setter Property="Height" Value="34"/>
        </Style>

        <Style x:Key="MonthLabelBorderStyle" TargetType="Border">
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="BorderThickness" Value="0,0.5,0.5,0.5"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Width" Value="110"/>
            <Setter Property="Height" Value="34"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- TOP BAR -->
        <Grid Margin="0,0,0,25">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <ToggleButton x:Name="HamburgerButton" Style="{StaticResource HamburgerButtonStyle}" Margin="0,0,15,0"/>

            <Popup IsOpen="{Binding IsChecked, ElementName=HamburgerButton}" 
                   PlacementTarget="{Binding ElementName=HamburgerButton}" 
                   StaysOpen="False" AllowsTransparency="True" PopupAnimation="Fade">
                <Border Background="White" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="8" Margin="0,5,0,0">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="15" ShadowDepth="2" Opacity="0.1"/>
                    </Border.Effect>
                    <StackPanel Margin="5" Width="220">
                        <TextBlock Text="NAVIGATE" FontSize="11" FontWeight="Bold" Foreground="#94A3B8" Margin="15,10,15,5"/>
                        <ItemsControl ItemsSource="{Binding NavigationMenuItems}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Button Content="{Binding Name}" Command="{Binding NavigateCommand}" Style="{StaticResource ToolMenuButtonStyle}"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>
            </Popup>

            <TextBlock Grid.Column="1" Text="Activity Heatmap Tracker" FontSize="22" FontWeight="Bold" Foreground="#0F172A" VerticalAlignment="Center"/>
        </Grid>

        <!-- EVENT GRIDS LIST -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding EventGroups}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Margin="0,0,0,50">
                            <!-- Action Label Banner (Refined Style) -->
                            <Border Background="#F3F4F6" Padding="15,10" BorderBrush="#E5E7EB" BorderThickness="1,1,1,0">
                                <TextBlock Text="{Binding EventTitle}" FontSize="15" FontWeight="Bold" Foreground="#111827"/>
                            </Border>

                            <!-- The Grid Table Container -->
                            <StackPanel HorizontalAlignment="Left" Background="White">
                                <!-- Header Row -->
                                <StackPanel Orientation="Horizontal">
                                    <!-- Label Month / Date -->
                                    <Border Style="{StaticResource HeaderBorderStyle}" Width="110">
                                        <TextBlock Text="Month / Date" FontSize="11" FontWeight="Bold" Foreground="#4B5563" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>

                                    <!-- Numbers 1-31 -->
                                    <ItemsControl ItemsSource="{Binding DayNumbers}">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <StackPanel Orientation="Horizontal"/>
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Border Style="{StaticResource HeaderBorderStyle}">
                                                    <TextBlock Text="{Binding StringFormat='\{0:00\}'}" FontSize="12" Foreground="#111827" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </StackPanel>

                                <!-- Month Rows -->
                                <ItemsControl ItemsSource="{Binding Months}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal">
                                                <!-- Month Label -->
                                                <Border Style="{StaticResource MonthLabelBorderStyle}">
                                                    <TextBlock Text="{Binding MonthLabel}" FontSize="11" FontWeight="SemiBold" Foreground="#4B5563" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Border>

                                                <!-- Day Cells -->
                                                <ItemsControl ItemsSource="{Binding Cells}">
                                                    <ItemsControl.ItemsPanel>
                                                        <ItemsPanelTemplate>
                                                            <StackPanel Orientation="Horizontal"/>
                                                        </ItemsPanelTemplate>
                                                    </ItemsControl.ItemsPanel>
                                                    <ItemsControl.ItemTemplate>
                                                        <DataTemplate>
                                                            <Border>
                                                                <Border.Style>
                                                                    <Style TargetType="Border" BasedOn="{StaticResource GridBorderStyle}">
                                                                        <Style.Triggers>
                                                                            <DataTrigger Binding="{Binding IsHidden}" Value="True">
                                                                                <Setter Property="Background" Value="#F9FAFB"/>
                                                                            </DataTrigger>
                                                                        </Style.Triggers>
                                                                    </Style>
                                                                </Border.Style>
                                                                
                                                                <!-- Activity Dot: Perfectly centered circle -->
                                                                <Grid VerticalAlignment="Center" HorizontalAlignment="Center">
                                                                    <Border Width="10" Height="10" Background="{Binding BgColor}" CornerRadius="5" 
                                                                            Visibility="{Binding IsActive, Converter={StaticResource BooleanToVisibilityConverter}}"
                                                                            ToolTip="{Binding Tooltip}"/>
                                                                </Grid>
                                                            </Border>
                                                        </DataTemplate>
                                                    </ItemsControl.ItemTemplate>
                                                </ItemsControl>
                                            </StackPanel>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
