<UserControl x:Class="Lifes.Presentation.WPF.Features.AnnualCalendar.MementoManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Lifes.Presentation.WPF.Features.AnnualCalendar"
             mc:Ignorable="d" 
             Background="#F8FAFC"
             d:DesignHeight="600" d:DesignWidth="1000">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="260"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Left Navigation Panel -->
        <Border Grid.Column="0" Background="White" BorderBrush="#E2E8F0" BorderThickness="0,0,1,0">
            <Grid Margin="0,24,0,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Text="CÔNG CỤ" FontSize="11" FontWeight="Bold" Foreground="#A0AEC0" Margin="24,0,24,12"/>

                <ItemsControl Grid.Row="1" ItemsSource="{Binding NavigationMenuItems}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button Command="{Binding NavigateCommand}" Margin="12,2,12,2" Height="40" Background="Transparent" BorderThickness="0" Cursor="Hand">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="Button">
                                                    <Border x:Name="border" Background="Transparent" CornerRadius="8" Padding="12,0">
                                                        <StackPanel Orientation="Horizontal">
                                                            <TextBlock x:Name="text" Text="{Binding Name}" VerticalAlignment="Center" FontSize="13" Foreground="#4A5568" FontWeight="Medium"/>
                                                        </StackPanel>
                                                    </Border>
                                                    <ControlTemplate.Triggers>
                                                        <DataTrigger Binding="{Binding IsActive}" Value="True">
                                                            <Setter TargetName="border" Property="Background" Value="#EBF8FF"/>
                                                            <Setter TargetName="text" Property="Foreground" Value="#3182CE"/>
                                                            <Setter TargetName="text" Property="FontWeight" Value="SemiBold"/>
                                                        </DataTrigger>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter TargetName="border" Property="Background" Value="#F7FAFC"/>
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </Button.Style>
                            </Button>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Grid>
        </Border>

        <!-- Content Area -->
        <Grid Grid.Column="1" Margin="32">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <StackPanel Grid.Row="0" Margin="0,0,0,24">
                <TextBlock Text="Quản lý Chủ đề (Topics)" FontSize="24" FontWeight="Bold" Foreground="#2D3748"/>
                <TextBlock Text="Sắp xếp và quản lý các đầu mục lớn trên lịch của bạn" FontSize="14" Foreground="#718096" Margin="0,4,0,0"/>
            </StackPanel>

            <!-- Filters Bar -->
            <Border Grid.Row="1" Background="White" CornerRadius="12" Padding="20" Margin="0,0,0,24" BorderBrush="#E2E8F0" BorderThickness="1">
                <StackPanel>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        
                        <StackPanel Orientation="Horizontal">
                            <!-- Search Box -->
                            <Border Background="#F1F5F9" CornerRadius="8" Padding="12,6" Margin="0,0,24,0" Width="250">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="🔍" VerticalAlignment="Center" Margin="0,0,8,0" Foreground="#94A3B8"/>
                                    <TextBox Grid.Column="1" 
                                             Text="{Binding SearchKeyword, UpdateSourceTrigger=PropertyChanged}" 
                                             Background="Transparent" 
                                             BorderThickness="0" 
                                             FontSize="13" 
                                             VerticalContentAlignment="Center"
                                             Foreground="#1E293B">
                                        <TextBox.Style>
                                            <Style TargetType="TextBox">
                                                <Style.Resources>
                                                    <VisualBrush x:Key="HintBrush" TileMode="None" Opacity="0.5" Stretch="None" AlignmentX="Left">
                                                        <VisualBrush.Visual>
                                                            <TextBlock Text="Tìm kiếm tiêu đề..."/>
                                                        </VisualBrush.Visual>
                                                    </VisualBrush>
                                                </Style.Resources>
                                                <Style.Triggers>
                                                    <Trigger Property="Text" Value="{x:Null}">
                                                        <Setter Property="Background" Value="{StaticResource HintBrush}"/>
                                                    </Trigger>
                                                    <Trigger Property="Text" Value="">
                                                        <Setter Property="Background" Value="{StaticResource HintBrush}"/>
                                                    </Trigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBox.Style>
                                    </TextBox>
                                </Grid>
                            </Border>

                            <TextBlock Text="Lọc theo Tag:" VerticalAlignment="Center" Foreground="#718096" FontSize="13" Margin="0,0,16,0"/>
                            <ItemsControl ItemsSource="{Binding AvailableTags}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <WrapPanel Orientation="Horizontal"/>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <CheckBox IsChecked="{Binding IsSelected}" Margin="0,0,8,4">
                                            <CheckBox.Template>
                                                <ControlTemplate TargetType="CheckBox">
                                                    <Border x:Name="TagBorder" Padding="8,4" CornerRadius="6" 
                                                            Background="#F7FAFC" BorderBrush="#E2E8F0" BorderThickness="1" Cursor="Hand">
                                                        <StackPanel Orientation="Horizontal">
                                                            <Ellipse Width="8" Height="8" Fill="{Binding Color}" Margin="0,0,6,0"/>
                                                            <TextBlock Text="{Binding Name}" Foreground="#4A5568" FontSize="11"/>
                                                        </StackPanel>
                                                    </Border>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsChecked" Value="True">
                                                            <Setter TargetName="TagBorder" Property="Background" Value="#EBF8FF"/>
                                                            <Setter TargetName="TagBorder" Property="BorderBrush" Value="#3182CE"/>
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </CheckBox.Template>
                                        </CheckBox>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>

                        <StackPanel Grid.Column="1" Orientation="Horizontal">
                            <CheckBox IsChecked="{Binding ParentOnly}" Content="Chỉ Topics cha" VerticalAlignment="Center" Margin="0,0,16,0"/>
                            <CheckBox IsChecked="{Binding IncludeChildren}" Content="Hiện cả con" VerticalAlignment="Center" Margin="0,0,16,0"/>
                            <Button Content="Tải lại" Command="{Binding LoadDataCommand}" Padding="16,6" Background="#EDF2F7" BorderThickness="0" Cursor="Hand"/>
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- Grid -->
            <Border Grid.Row="2" Background="White" CornerRadius="12" BorderBrush="#E2E8F0" BorderThickness="1">
                <DataGrid ItemsSource="{Binding Mementos}" 
                          AutoGenerateColumns="False" 
                          CanUserAddRows="False" 
                          CanUserSortColumns="True"
                          Background="Transparent"
                          BorderThickness="0"
                          RowHeight="40"
                          HeadersVisibility="Column"
                          GridLinesVisibility="Horizontal"
                          HorizontalGridLinesBrush="#F1F5F9"
                          RowHeaderWidth="0">
                    <DataGrid.Resources>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#F8FAFC"/>
                            <Setter Property="Foreground" Value="#64748B"/>
                            <Setter Property="Padding" Value="16,12"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="BorderThickness" Value="0,0,0,1"/>
                            <Setter Property="BorderBrush" Value="#E2E8F0"/>
                        </Style>
                        <Style TargetType="DataGridCell">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="DataGridCell">
                                        <Border x:Name="CellBorder" Padding="16,0" Background="Transparent" BorderThickness="0">
                                            <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Left"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter TargetName="CellBorder" Property="Background" Value="#F1F5F9"/>
                                                <Setter Property="Foreground" Value="#1E293B"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </DataGrid.Resources>
                    <DataGrid.Columns>
                        <DataGridTemplateColumn Header="TITLE" Width="2*" SortMemberPath="Title">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Grid Background="Transparent">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="{Binding Title}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                                        <Button Grid.Column="1" 
                                                x:Name="EditButton"
                                                Command="{Binding DataContext.EditMementoCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                CommandParameter="{Binding}"
                                                Content="✎" 
                                                Margin="8,0,0,0"
                                                Padding="4,2"
                                                Background="Transparent" 
                                                BorderThickness="0" 
                                                Foreground="#3182CE"
                                                FontSize="14"
                                                Cursor="Hand"
                                                Visibility="Collapsed"
                                                ToolTip="Chỉnh sửa chi tiết">
                                            <Button.Template>
                                                <ControlTemplate TargetType="Button">
                                                    <Border x:Name="BtnBorder" Background="Transparent" CornerRadius="4" Padding="{TemplateBinding Padding}">
                                                        <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                                    </Border>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter TargetName="BtnBorder" Property="Background" Value="#EBF8FF"/>
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Button.Template>
                                        </Button>
                                    </Grid>
                                    <DataTemplate.Triggers>
                                        <DataTrigger Binding="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=DataGridRow}}" Value="True">
                                            <Setter TargetName="EditButton" Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </DataTemplate.Triggers>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTextColumn Header="DESCRIPTION" Binding="{Binding Description}" Width="3*" IsReadOnly="True" SortMemberPath="Description"/>
                        <DataGridTextColumn Header="START" Binding="{Binding StartDate, StringFormat=dd/MM/yyyy}" Width="120" IsReadOnly="True" SortMemberPath="StartDate"/>
                        <DataGridTextColumn Header="END" Binding="{Binding EndDate, StringFormat=dd/MM/yyyy}" Width="120" IsReadOnly="True" SortMemberPath="EndDate"/>
                        <DataGridTemplateColumn Header="ORDER" Width="100" SortMemberPath="Order">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Order}" FontWeight="Bold" Foreground="#3182CE"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                            <DataGridTemplateColumn.CellEditingTemplate>
                                <DataTemplate>
                                    <TextBox Text="{Binding Order, UpdateSourceTrigger=PropertyChanged}" VerticalContentAlignment="Center"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellEditingTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTextColumn Header="CREATED" Binding="{Binding CreatedDate, StringFormat=dd/MM/yyyy}" Width="120" IsReadOnly="True" SortMemberPath="CreatedDate"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>

            <!-- Footer Actions -->
            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,24,0,0">
                <Button Content="Xác nhận thay đổi thứ tự" 
                        Command="{Binding SaveChangesCommand}"
                        Padding="24,12" Background="#3182CE" Foreground="White" 
                        FontWeight="Bold" BorderThickness="0" Cursor="Hand">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="8"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>
        </Grid>

        <!-- Edit Topic Popup -->
        <Popup IsOpen="{Binding IsTopicEditorOpen}" StaysOpen="True" Placement="Center" AllowsTransparency="True" PopupAnimation="Fade">
            <Border Background="Transparent">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="30" ShadowDepth="10" Opacity="0.3"/>
                </Border.Effect>
                <local:TopicEditorView DataContext="{Binding TopicEditor}" Width="400"/>
            </Border>
        </Popup>
    </Grid>
</UserControl>
