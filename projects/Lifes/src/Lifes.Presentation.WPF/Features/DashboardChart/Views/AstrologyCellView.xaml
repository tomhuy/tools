<UserControl x:Class="Lifes.Presentation.WPF.Features.DashboardChart.Views.AstrologyCellView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignWidth="200" d:DesignHeight="150">
    <Border BorderBrush="#303040" BorderThickness="1" CornerRadius="8" Margin="4" Padding="12" Background="#252535">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="{Binding Data.HeaderText}" Foreground="#A0A0B0" FontSize="12"/>
                <TextBlock Grid.Column="1" Text="{Binding Data.MinorText}" Foreground="#606070" FontSize="11"/>
            </Grid>
            
            <TextBlock Grid.Row="1" Text="{Binding Name}" Foreground="#3ECF8E" FontSize="16" FontWeight="Bold" Margin="0,8,0,4"/>
            
            <ItemsControl Grid.Row="2" ItemsSource="{Binding Data.Elements}" Margin="0,4,0,0">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="#303040" CornerRadius="4" Margin="0,0,4,4" Padding="6,2">
                            <TextBlock Text="{Binding}" FontSize="12" Foreground="#D0D0E0"/>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Grid>
    </Border>
</UserControl>
